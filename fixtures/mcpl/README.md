# mcpl — Synthetic MCPL interchange fixtures authored for Nucleide (no license needed)

Conversion fixtures live in `ssw_conversion/` (see its README for the
provenance table): a hand-framed two-track synthetic SSW reference plus the
golden `ssw2mcpl` output over the documented surface/kind pairing. No MCNP
runs, no vendored upstream bytes.

Merge/extract golden pairs for the particle-list utilities live in `utils/`
(see its README for the per-file provenance table): hand-framed inputs and
golden merged/extracted outputs, all produced by the Nucleide writers.
